openapi: 3.2.0
info:
  title: Purplebricks.Valuations.Api 1.0 - Core Salesforce Diary…
  description: '### The new home for accessing the valuations service library

    ### Purplebricks.Valuations.Api Git Repository'
  version: '1.0'
servers:
- url: https://api.purplebricks.co.uk/valuations-api
security:
- Bearer: []
tags:
- name: SalesforceDiaryPilot
paths:
  /v1/salesforcediarypilot/isindiarymanagementtrial/{postcode}:
    get:
      tags:
      - SalesforceDiaryPilot
      parameters:
      - name: postcode
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            text/plain:
              schema:
                type: boolean
            application/json:
              schema:
                type: boolean
            text/json:
              schema:
                type: boolean
      summary: Get salesforcediarypilot isindiarymanagementtrial by postcode
      x-summary-source: derived
      operationId: getV1SalesforcediarypilotIsindiarymanagementtrialByPostcode
      x-operation-id-source: derived
components:
  securitySchemes:
    Bearer:
      type: apiKey
      description: Authorization header using Bearer scheme
      name: Authorization
      in: header
